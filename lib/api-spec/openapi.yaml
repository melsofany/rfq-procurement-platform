openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: RFQ Management System API for Cortoba Supplies
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: auth
    description: Authentication
  - name: rfq
    description: RFQ management
  - name: suppliers
    description: Supplier management
  - name: customers
    description: Customer management
  - name: customer-rfq
    description: Customer Request for Quotation intake
  - name: customer-po
    description: Customer purchase orders
  - name: offers
    description: Supplier offers/pricing
  - name: analytics
    description: Analytics and dashboards
  - name: audit
    description: Audit log
  - name: integrations
    description: "تكامل مع أنظمة ERP (Odoo · SAP · Oracle · Google Sheets)"

paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  # AUTH
  /auth/login:
    post:
      operationId: login
      tags: [auth]
      summary: Employee login
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginInput"
      responses:
        "200":
          description: Login successful
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "401":
          description: Invalid credentials

  /auth/logout:
    post:
      operationId: logout
      tags: [auth]
      summary: Logout
      responses:
        "200":
          description: Logged out

  /auth/me:
    get:
      operationId: getMe
      tags: [auth]
      summary: Get current user
      responses:
        "200":
          description: Current user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Employee"
        "401":
          description: Unauthorized

  # EMPLOYEES
  /employees:
    get:
      operationId: listEmployees
      tags: [auth]
      summary: List all employees
      responses:
        "200":
          description: List of employees
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Employee"
    post:
      operationId: createEmployee
      tags: [auth]
      summary: Create employee
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EmployeeInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Employee"

  /employees/{id}:
    patch:
      operationId: updateEmployee
      tags: [auth]
      summary: Update employee
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EmployeeUpdate"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Employee"

  # SUPPLIERS
  /categories:
    get:
      operationId: listCategories
      tags: [suppliers]
      summary: List supplier categories
      responses:
        "200":
          description: List of categories
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SupplierCategory"
    post:
      operationId: createCategory
      tags: [suppliers]
      summary: Create supplier category
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CategoryInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupplierCategory"

  /categories/{id}:
    patch:
      operationId: updateCategory
      tags: [suppliers]
      summary: Update supplier category
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CategoryInput"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupplierCategory"
    delete:
      operationId: deleteCategory
      tags: [suppliers]
      summary: Delete supplier category
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  /suppliers:
    get:
      operationId: listSuppliers
      tags: [suppliers]
      summary: List suppliers with optional category filter
      parameters:
        - name: category
          in: query
          schema:
            type: string
        - name: search
          in: query
          schema:
            type: string
      responses:
        "200":
          description: List of suppliers
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Supplier"
    post:
      operationId: createSupplier
      tags: [suppliers]
      summary: Create supplier
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SupplierInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Supplier"

  /suppliers/{id}:
    get:
      operationId: getSupplier
      tags: [suppliers]
      summary: Get supplier
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Supplier
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Supplier"
    patch:
      operationId: updateSupplier
      tags: [suppliers]
      summary: Update supplier
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SupplierUpdate"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Supplier"

  /suppliers/bulk:
    post:
      operationId: bulkImportSuppliers
      tags: [suppliers]
      summary: "استيراد موردين بالجملة"
      description: "إضافة عدة موردين دفعة واحدة مع تعيين التصنيف لكل منهم"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BulkImportSuppliersInput"
      responses:
        "200":
          description: نتيجة الاستيراد
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BulkImportSuppliersResult"

  /suppliers/scores:
    get:
      operationId: listSupplierScores
      tags: [suppliers]
      summary: "تقييمات جميع الموردين مرتبةً تنازلياً"
      responses:
        "200":
          description: قائمة تقييمات الموردين
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SupplierScore"

  /suppliers/{id}/score:
    get:
      operationId: getSupplierScore
      tags: [suppliers]
      summary: Get supplier performance score
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Supplier score
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupplierScore"

  # CUSTOMERS
  /customers:
    get:
      operationId: listCustomers
      tags: [customers]
      summary: List customers with optional search
      parameters:
        - name: search
          in: query
          schema:
            type: string
        - name: active
          in: query
          schema:
            type: string
            enum: ["true", "false"]
      responses:
        "200":
          description: List of customers
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Customer"
    post:
      operationId: createCustomer
      tags: [customers]
      summary: Create customer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CustomerInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Customer"

  /customers/{id}:
    get:
      operationId: getCustomer
      tags: [customers]
      summary: Get customer
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Customer
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Customer"
    patch:
      operationId: updateCustomer
      tags: [customers]
      summary: Update customer
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CustomerUpdate"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Customer"
    delete:
      operationId: deleteCustomer
      tags: [customers]
      summary: Delete customer (admin/manager only)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "409":
          description: Cannot delete (linked records exist)

  # CUSTOMER RFQ
  /customer-rfq:
    get:
      operationId: listCustomerRfqs
      tags: [customer-rfq]
      summary: List customer RFQs with optional search
      parameters:
        - name: search
          in: query
          schema:
            type: string
        - name: status
          in: query
          schema:
            type: string
      responses:
        "200":
          description: List of customer RFQs
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CustomerRfq"
    post:
      operationId: createCustomerRfq
      tags: [customer-rfq]
      summary: Create a customer RFQ (auto-generates number when blank)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CustomerRfqInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerRfq"

  /customer-rfq/numbers:
    get:
      operationId: listCustomerRfqNumbers
      tags: [customer-rfq]
      summary: List all customer RFQ numbers (for the supplier-RFQ import combobox)
      responses:
        "200":
          description: List of customer RFQ numbers
          content:
            application/json:
              schema:
                type: object
                properties:
                  rfqNumbers:
                    type: array
                    items:
                      type: string
                required: [rfqNumbers]

  /customer-rfq/sheet-view:
    get:
      operationId: listCustomerRfqSheetView
      tags: [customer-rfq]
      summary: Flat sheet-style view (one row per customer RFQ item with joined PO columns)
      parameters:
        - name: search
          in: query
          schema:
            type: string
          description: Global OR search across the main text columns
        - name: lineItemExclude
          in: query
          schema: { type: string }
          description: Comma-separated values to HIDE from this column (Excel autofilter)
        - name: partNoExclude
          in: query
          schema: { type: string }
        - name: descriptionExclude
          in: query
          schema: { type: string }
        - name: uomExclude
          in: query
          schema: { type: string }
        - name: customerRfqNoExclude
          in: query
          schema: { type: string }
        - name: customerNameExclude
          in: query
          schema: { type: string }
        - name: entryDateExclude
          in: query
          schema: { type: string }
        - name: expiryDateExclude
          in: query
          schema: { type: string }
        - name: buyerNameExclude
          in: query
          schema: { type: string }
        - name: poNoExclude
          in: query
          schema: { type: string }
        - name: poDateExclude
          in: query
          schema: { type: string }
        - name: rfqQtyExclude
          in: query
          schema: { type: string }
        - name: rfqUnitPriceExclude
          in: query
          schema: { type: string }
        - name: poQtyExclude
          in: query
          schema: { type: string }
        - name: poUnitPriceExclude
          in: query
          schema: { type: string }
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 500
            default: 100
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
      responses:
        "200":
          description: Paginated flat sheet-style rows
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerRfqSheetView"
  /customer-rfq/sheet-view/facets:
    get:
      operationId: listCustomerRfqSheetViewFacets
      tags: [customer-rfq]
      summary: Distinct values (with counts) for one sheet-view column, after applying other columns' filters
      parameters:
        - name: column
          in: query
          required: true
          schema:
            type: string
            enum: [lineItem, partNo, description, uom, customerRfqNo, customerName, entryDate, expiryDate, buyerName, poNo, poDate, rfqQty, rfqUnitPrice, poQty, poUnitPrice]
          description: The column to compute distinct values for
        - name: search
          in: query
          schema: { type: string }
        - name: lineItemExclude
          in: query
          schema: { type: string }
        - name: partNoExclude
          in: query
          schema: { type: string }
        - name: descriptionExclude
          in: query
          schema: { type: string }
        - name: uomExclude
          in: query
          schema: { type: string }
        - name: customerRfqNoExclude
          in: query
          schema: { type: string }
        - name: customerNameExclude
          in: query
          schema: { type: string }
        - name: entryDateExclude
          in: query
          schema: { type: string }
        - name: expiryDateExclude
          in: query
          schema: { type: string }
        - name: buyerNameExclude
          in: query
          schema: { type: string }
        - name: poNoExclude
          in: query
          schema: { type: string }
        - name: poDateExclude
          in: query
          schema: { type: string }
        - name: rfqQtyExclude
          in: query
          schema: { type: string }
        - name: rfqUnitPriceExclude
          in: query
          schema: { type: string }
        - name: poQtyExclude
          in: query
          schema: { type: string }
        - name: poUnitPriceExclude
          in: query
          schema: { type: string }
      responses:
        "200":
          description: Distinct values with counts
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerRfqSheetFacets"
        "400":
          description: Invalid or missing column

  /customer-rfq/{id}:
    get:
      operationId: getCustomerRfq
      tags: [customer-rfq]
      summary: Get customer RFQ with items
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Customer RFQ
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerRfqDetail"
        "404":
          description: Not found
    patch:
      operationId: updateCustomerRfq
      tags: [customer-rfq]
      summary: Update a draft customer RFQ
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CustomerRfqUpdate"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerRfqDetail"
    delete:
      operationId: deleteCustomerRfq
      tags: [customer-rfq]
      summary: Delete customer RFQ
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "404":
          description: Not found

  # CUSTOMER PO
  /customer-po:
    get:
      operationId: listCustomerPos
      tags: [customer-po]
      summary: List customer POs with optional search
      parameters:
        - name: search
          in: query
          schema:
            type: string
        - name: status
          in: query
          schema:
            type: string
      responses:
        "200":
          description: List of customer POs
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CustomerPo"
    post:
      operationId: createCustomerPo
      tags: [customer-po]
      summary: Create a customer PO (auto-generates the internal number)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CustomerPoInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerPo"
        "400":
          description: Validation error

  /customer-po/customer-rfqs:
    get:
      operationId: listCustomerPosCustomerRfqs
      tags: [customer-po]
      summary: Light list of customer RFQs for the customer-PO picker
      responses:
        "200":
          description: List of customer RFQs
          content:
            application/json:
              schema:
                type: object
                properties:
                  rfqs:
                    type: array
                    items:
                      $ref: "#/components/schemas/CustomerPoCustomerRfqOption"
                required: [rfqs]

  /customer-po/{id}:
    get:
      operationId: getCustomerPo
      tags: [customer-po]
      summary: Get customer PO with items
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Customer PO
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerPoDetail"
        "404":
          description: Not found
    patch:
      operationId: updateCustomerPo
      tags: [customer-po]
      summary: Update a draft customer PO (or finalize → sent)
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CustomerPoUpdate"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerPoDetail"
        "400":
          description: PO is not a draft or validation failed
        "404":
          description: Not found
    delete:
      operationId: deleteCustomerPo
      tags: [customer-po]
      summary: Delete a draft customer PO
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "400":
          description: PO is not a draft (already sent)
        "404":
          description: Not found

  # ERP INTEGRATIONS
  /integrations:
    get:
      operationId: listIntegrations
      tags: [integrations]
      summary: "قائمة تكاملات الـ ERP"
      responses:
        "200":
          description: قائمة التكاملات
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ErpIntegration"
    post:
      operationId: createIntegration
      tags: [integrations]
      summary: "إنشاء تكامل جديد"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ErpIntegrationInput"
      responses:
        "201":
          description: تم الإنشاء
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErpIntegration"

  /integrations/{id}:
    get:
      operationId: getIntegration
      tags: [integrations]
      summary: "تفاصيل تكامل"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: التكامل
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErpIntegration"
    patch:
      operationId: updateIntegration
      tags: [integrations]
      summary: "تعديل تكامل"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ErpIntegrationInput"
      responses:
        "200":
          description: تم التعديل
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErpIntegration"
    delete:
      operationId: deleteIntegration
      tags: [integrations]
      summary: "حذف تكامل"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: تم الحذف

  /integrations/{id}/test:
    post:
      operationId: testIntegrationConnection
      tags: [integrations]
      summary: "اختبار الاتصال بالـ ERP"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: نتيجة الاختبار
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErpConnectionTestResult"

  /integrations/{id}/sync:
    post:
      operationId: syncIntegration
      tags: [integrations]
      summary: "تشغيل مزامنة كاملة (في الخلفية)"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: بدأت المزامنة
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  integrationId:
                    type: integer

  /integrations/{id}/sync-suppliers:
    post:
      operationId: syncIntegrationSuppliers
      tags: [integrations]
      summary: "استيراد الموردين من الـ ERP"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: نتيجة الاستيراد
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErpSyncResult"

  /integrations/{id}/export:
    post:
      operationId: exportToIntegration
      tags: [integrations]
      summary: "تصدير البيانات للـ ERP"
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: نتيجة التصدير
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErpSyncResult"

  # RFQ
  /rfq:
    get:
      operationId: listRfqs
      tags: [rfq]
      summary: List all RFQs
      parameters:
        - name: status
          in: query
          schema:
            type: string
        - name: employeeId
          in: query
          schema:
            type: integer
        - name: search
          in: query
          schema:
            type: string
      responses:
        "200":
          description: List of RFQs
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Rfq"
    post:
      operationId: createRfq
      tags: [rfq]
      summary: Create a new RFQ from customer RFQ number
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RfqInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Rfq"

  /rfq/{id}:
    get:
      operationId: getRfq
      tags: [rfq]
      summary: Get RFQ by ID
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: RFQ
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Rfq"
    patch:
      operationId: updateRfq
      tags: [rfq]
      summary: Update RFQ status
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RfqUpdate"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Rfq"

  /rfq/{id}/items:
    get:
      operationId: listRfqItems
      tags: [rfq]
      summary: Get items for an RFQ
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: RFQ items
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/RfqItem"

  /rfq/{id}/send:
    post:
      operationId: sendRfqToSuppliers
      tags: [rfq]
      summary: Send RFQ to selected suppliers via email
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendRfqInput"
      responses:
        "200":
          description: Sent
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SendRfqResult"

  /rfq/{id}/sent-log:
    get:
      operationId: getRfqSentLog
      tags: [rfq]
      summary: Get sent log for an RFQ
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Sent log entries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SentLogEntry"

  /rfq/{id}/offers:
    get:
      operationId: getRfqOffers
      tags: [rfq]
      summary: Get all offers for an RFQ with price analysis
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Offers with analysis
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RfqOffersResponse"

  /offers/items/{offerItemId}/approve:
    patch:
      operationId: approveOfferItem
      tags: [offers]
      summary: Approve (endorse) a supplier price for an rfq_item — only one approved price per rfq_item.
      parameters:
        - name: offerItemId
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                approved:
                  type: boolean
      responses:
        "200":
          description: Approval toggled
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: integer
                  isApproved:
                    type: boolean

  /rfq/lookup/{customerRfqNo}:
    get:
      operationId: lookupCustomerRfq
      tags: [rfq]
      summary: Lookup items from customer RFQ number (reads Google Sheets DATA sheet)
      parameters:
        - name: customerRfqNo
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Customer RFQ items
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CustomerRfqItem"

  # PURCHASE ORDERS
  /po:
    get:
      operationId: listPurchaseOrders
      tags: [po]
      summary: List all purchase orders
      parameters:
        - name: status
          in: query
          schema:
            type: string
        - name: search
          in: query
          schema:
            type: string
      responses:
        "200":
          description: List of purchase orders
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PurchaseOrder"
    post:
      operationId: createPurchaseOrder
      tags: [po]
      summary: Create a new purchase order from a sheet PO number, with per-item supplier assignment
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PurchaseOrderInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PurchaseOrder"

  /po/{id}:
    get:
      operationId: getPurchaseOrder
      tags: [po]
      summary: Get purchase order by ID
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Purchase order
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PurchaseOrder"
    put:
      operationId: updatePurchaseOrder
      tags: [po]
      summary: Update a DRAFT purchase order (PO-level fields + full items replacement)
      description: |
        Only purchase orders still in "draft" status may be edited. Once a PO is
        dispatched ("sent") it is locked. The supplied `items` array fully replaces
        the existing items, so add/edit/remove are all handled by sending the new list.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PurchaseOrderUpdateInput"
      responses:
        "200":
          description: Updated purchase order
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PurchaseOrder"
        "400":
          description: PO is not a draft or validation failed
        "404":
          description: Purchase order not found
    delete:
      operationId: deletePurchaseOrder
      tags: [po]
      summary: Delete a DRAFT purchase order and all its line items
      description: |
        Only purchase orders still in "draft" status may be deleted. Once a PO is
        dispatched ("sent") it is locked and cannot be removed. Deleting a draft PO
        also removes its line items and any work-order assignments in a single
        transaction.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Purchase order deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  id:
                    type: integer
        "400":
          description: PO is not a draft (already sent)
        "404":
          description: Purchase order not found

  /po/{id}/items:
    get:
      operationId: listPurchaseOrderItems
      tags: [po]
      summary: Get items for a purchase order
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Purchase order items
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PurchaseOrderItem"

  /po/lookup/{poNo}:
    get:
      operationId: lookupPoFromSheet
      tags: [po]
      summary: Lookup items from a purchase order number (reads Google Sheets DATA sheet, column K)
      parameters:
        - name: poNo
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Purchase order items from sheet
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SheetPoItem"

  /po/sheets/po-numbers:
    get:
      operationId: listSheetPoNumbers
      tags: [po]
      summary: List unique purchase order numbers found in the sheet (column K)
      parameters:
        - name: sheet
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Purchase order numbers
          content:
            application/json:
              schema:
                type: object
                required: [poNumbers]
                properties:
                  poNumbers:
                    type: array
                    items:
                      type: string

  # PRICING TOKEN (Supplier Portal)
  /pricing/{token}:
    get:
      operationId: getPricingPage
      tags: [offers]
      summary: Get pricing page data by token (public - for suppliers)
      parameters:
        - name: token
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Pricing page data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PricingPageData"
        "404":
          description: Token not found or expired

  /pricing/{token}/submit:
    post:
      operationId: submitOffer
      tags: [offers]
      summary: Submit offer via pricing token (public - for suppliers)
      parameters:
        - name: token
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OfferInput"
      responses:
        "201":
          description: Offer submitted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Offer"
        "400":
          description: Token expired or invalid

  /pricing/{token}/track:
    post:
      operationId: trackLinkOpen
      tags: [offers]
      summary: Track when supplier opens the pricing link
      parameters:
        - name: token
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Tracked

  # ITEMS SEARCH
  /items/search:
    get:
      operationId: searchItems
      tags: [rfq]
      summary: Search items across all RFQs by description, part number, or line item
      parameters:
        - name: q
          in: query
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Item search results
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ItemHistory"

  # OFFERS
  /offers:
    get:
      operationId: listOffers
      tags: [offers]
      summary: List all offers
      parameters:
        - name: rfqId
          in: query
          schema:
            type: integer
        - name: supplierId
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: List of offers
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Offer"

  # ANALYTICS
  /analytics/dashboard:
    get:
      operationId: getDashboardStats
      tags: [analytics]
      summary: Get dashboard statistics
      responses:
        "200":
          description: Dashboard stats
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardStats"

  /analytics/employee/{id}:
    get:
      operationId: getEmployeePerformance
      tags: [analytics]
      summary: Get employee performance metrics
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Employee performance
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeePerformance"

  /analytics/price-analysis/{rfqId}:
    get:
      operationId: getPriceAnalysis
      tags: [analytics]
      summary: Price analysis for an RFQ - compare offers, detect anomalies
      parameters:
        - name: rfqId
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Price analysis
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PriceAnalysis"


  /analytics/reports:
    get:
      operationId: getReports
      tags: [analytics]
      summary: 'تقارير شاملة مع فلترة بالتاريخ'
      parameters:
        - name: from
          in: query
          schema:
            type: string
            format: date
          description: 'تاريخ البداية (YYYY-MM-DD)'
        - name: to
          in: query
          schema:
            type: string
            format: date
          description: 'تاريخ النهاية (YYYY-MM-DD)'
      responses:
        '200':
          description: تقارير تفصيلية
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReportsData'

  # AUDIT LOG
  /audit:
    get:
      operationId: listAuditLogs
      tags: [audit]
      summary: List audit log entries
      parameters:
        - name: rfqId
          in: query
          schema:
            type: integer
        - name: employeeId
          in: query
          schema:
            type: integer
        - name: limit
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: Audit logs
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AuditLog"

components:
  schemas:
    HealthStatus:
      type: object
      required: [status]
      properties:
        status:
          type: string

    # AUTH
    LoginInput:
      type: object
      required: [email, password]
      properties:
        email:
          type: string
        password:
          type: string

    AuthResponse:
      type: object
      required: [employee, token]
      properties:
        employee:
          $ref: "#/components/schemas/Employee"
        token:
          type: string

    Employee:
      type: object
      required: [id, name, email, role]
      properties:
        id:
          type: integer
        name:
          type: string
        email:
          type: string
        role:
          type: string
          enum: [superadmin, admin, manager, purchasing, data_entry]
        phone:
          type: ["string", "null"]
        isActive:
          type: boolean
        permissions:
          type: ["object", "null"]
          description: "Per-employee permission map { permissionKey: true }. null = role default."
          additionalProperties:
            type: boolean
        createdAt:
          type: string

    EmployeeInput:
      type: object
      required: [name, email, password, role]
      properties:
        name:
          type: string
        email:
          type: string
        password:
          type: string
        role:
          type: string
          enum: [superadmin, admin, manager, purchasing, data_entry]
        phone:
          type: string
        permissions:
          type: ["object", "null"]
          description: "Per-employee permission map { permissionKey: true }. null = role default."
          additionalProperties:
            type: boolean

    EmployeeUpdate:
      type: object
      properties:
        name:
          type: string
        email:
          type: string
        role:
          type: string
          enum: [superadmin, admin, manager, purchasing, data_entry]
        phone:
          type: string
        isActive:
          type: boolean
        password:
          type: string
        permissions:
          type: ["object", "null"]
          description: "Per-employee permission map { permissionKey: true }. null = role default."
          additionalProperties:
            type: boolean

    # CATEGORIES
    SupplierCategory:
      type: object
      required: [id, name]
      properties:
        id:
          type: integer
        name:
          type: string

    CategoryInput:
      type: object
      required: [name]
      properties:
        name:
          type: string

    # SUPPLIERS
    Supplier:
      type: object
      required: [id, name, category]
      properties:
        id:
          type: integer
        supplierId:
          type: string
        name:
          type: string
        contactPerson:
          type: ["string", "null"]
        email:
          type: ["string", "null"]
        phone:
          type: ["string", "null"]
        address:
          type: ["string", "null"]
        category:
          type: string
        isActive:
          type: boolean
        createdAt:
          type: string

    SupplierInput:
      type: object
      required: [name, category]
      properties:
        supplierId:
          type: string
        name:
          type: string
        contactPerson:
          type: string
        email:
          type: string
        phone:
          type: string
        address:
          type: string
        category:
          type: string

    SupplierUpdate:
      type: object
      properties:
        name:
          type: string
        contactPerson:
          type: string
        email:
          type: string
        phone:
          type: string
        address:
          type: string
        category:
          type: string
        isActive:
          type: boolean

    # CUSTOMERS
    Customer:
      type: object
      required: [id, name, isActive]
      properties:
        id:
          type: integer
        customerId:
          type: ["string", "null"]
        name:
          type: string
        nickname:
          type: ["string", "null"]
        contactPerson:
          type: ["string", "null"]
        email:
          type: ["string", "null"]
        phone:
          type: ["string", "null"]
        address:
          type: ["string", "null"]
        taxId:
          type: ["string", "null"]
        notes:
          type: ["string", "null"]
        isActive:
          type: boolean
        createdAt:
          type: string

    CustomerInput:
      type: object
      required: [name, phone, address]
      properties:
        customerId:
          type: string
        name:
          type: string
        nickname:
          type: string
        contactPerson:
          type: string
        email:
          type: string
        phone:
          type: string
        address:
          type: string
        taxId:
          type: string
        notes:
          type: string

    CustomerUpdate:
      type: object
      properties:
        customerId:
          type: string
        name:
          type: string
        nickname:
          type: string
        contactPerson:
          type: string
        email:
          type: string
        phone:
          type: string
        address:
          type: string
        taxId:
          type: string
        notes:
          type: string
        isActive:
          type: boolean

    CustomerRfqSheetRow:
      type: object
      description: One flat row reproducing the legacy single-sheet layout — a customer RFQ line item with its joined customer PO columns (when a PO was issued).
      properties:
        rfqItemId:
          type: integer
        lineItem:
          type: ["string", "null"]
        partNo:
          type: ["string", "null"]
        description:
          type: ["string", "null"]
        uom:
          type: ["string", "null"]
        rfqQty:
          type: ["string", "null"]
        rfqUnitPrice:
          type: ["string", "null"]
          description: Price offered to the customer (from the customer RFQ item)
        customerRfqId:
          type: integer
        customerRfqNo:
          type: string
        customerName:
          type: string
        entryDate:
          type: ["string", "null"]
          description: Request date (تاريخ الطلب)
        expiryDate:
          type: ["string", "null"]
          description: Request expiry date (تاريخ انتهاء الطلب)
        buyerName:
          type: ["string", "null"]
          description: Buyer / employee at the customer's company (recorded with the RFQ)
        poItemId:
          type: ["integer", "null"]
        poNo:
          type: ["string", "null"]
        poDate:
          type: ["string", "null"]
        poQty:
          type: ["string", "null"]
        poUnitPrice:
          type: ["string", "null"]
        flagged:
          type: boolean
          description: True when the row has an issue (rejected customer delivery OR actual supplier cost exceeded the PO price OR a manual highlight note is set).
        flagReason:
          type: ["string", "null"]
          description: "Arabic reason for the flag — computed flags merged with the manual highlight note, e.g. 'رفض التسليم: تالف — مراجعة خاصة'."
        highlightColor:
          type: ["string", "null"]
          enum: [yellow, green, blue, red, orange, purple, null]
          description: Manual row tint color set on the customer PO line (renders the whole row in that color).

    CustomerRfqSheetView:
      type: object
      description: Paginated flat sheet-style view (one row per customer RFQ item, with joined PO columns).
      required: [total, limit, offset, rows]
      properties:
        total:
          type: integer
        limit:
          type: integer
        offset:
          type: integer
        rows:
          type: array
          items:
            $ref: "#/components/schemas/CustomerRfqSheetRow"
    CustomerRfqSheetFacetValue:
      type: object
      description: A distinct value for a sheet-view column with its occurrence count.
      required: [value, count]
      properties:
        value:
          type: string
          description: The distinct cell value (empty string for NULL/blank).
        count:
          type: integer
    CustomerRfqSheetFacets:
      type: object
      description: Distinct values (with counts) for one sheet-view column, computed after applying other columns' filters.
      required: [column, values]
      properties:
        column:
          type: string
        values:
          type: array
          items:
            $ref: "#/components/schemas/CustomerRfqSheetFacetValue"

    CustomerRfq:
      type: object
      required: [id, internalNo, customerName, customerRfqNo, numberAutoGenerated, status, createdAt, updatedAt]
      properties:
        id:
          type: integer
        internalNo:
          type: string
        customerId:
          type: ["integer", "null"]
        customerName:
          type: string
        customerRfqNo:
          type: string
        numberAutoGenerated:
          type: boolean
          description: True when the customer RFQ number was auto-generated (left blank by the user)
        entryDate:
          type: ["string", "null"]
        expiryDate:
          type: ["string", "null"]
        buyerName:
          type: ["string", "null"]
        employeeId:
          type: ["integer", "null"]
          description: Employee who entered the RFQ (auto from session)
        employeeName:
          type: ["string", "null"]
          description: Name of the employee who entered the RFQ
        status:
          type: string
        notes:
          type: ["string", "null"]
        itemCount:
          type: integer
        requestStatus:
          $ref: "#/components/schemas/CustomerRfqRequestStatus"
        createdAt:
          type: string
        updatedAt:
          type: string

    CustomerRfqRequestStatus:
      type: object
      description: >
        Derived, progressive request status (حالة الطلب) rolled up across offers,
        customer pricing, customer POs and deliveries. The headline stage/label
        prefers the most advanced milestone reached.
      required: [stage, label, supplierPriced, poIssued, poItemIds]
      properties:
        stage:
          type: string
          enum: [received, supplier_priced, customer_priced, po_issued, delivered, failed, expired]
          description: >
            received (default) | supplier_priced (an approved offer exists) |
            customer_priced (some items priced for the customer) | po_issued (a
            customer PO was issued for an item) | delivered (items delivered to the
            customer — نجح) | failed (items rejected at delivery or supplier
            receipt and NONE delivered — the request did not succeed) | expired
            (the close date passed with no item priced — failed).
        label:
          type: string
          description: Arabic label ready to display, e.g. "طلب وارد", "مُسعَّر من المورد", "مُسعَّر 50%", "صدر أمر شراء", "نجح 60%", "نجح بالكامل", "فشل", "منتهي (فشل)".
        supplierPriced:
          type: boolean
          description: True when at least one approved supplier offer exists for an item of this RFQ.
        customerPricingPct:
          type: ["integer", "null"]
          description: Share (0–100) of this RFQ's items priced for the customer (unit_price > 0). Null when the RFQ has no items.
        poIssued:
          type: boolean
          description: True when any customer_po_items row links back to an item of this RFQ.
        poItemIds:
          type: array
          items:
            type: integer
          description: customer_rfq_item_ids that already appear on a customer PO (used to highlight rows green in the detail page).
        deliveredPct:
          type: ["integer", "null"]
          description: "Share (0–100) of PO'd items successfully delivered to the customer (rejections excluded). Null when no PO was issued."
        failed:
          type: boolean
          description: True when at least one PO'd item was rejected (at delivery or supplier receipt) and NONE were delivered → the request failed.

    CustomerRfqLineItemInput:
      type: object
      properties:
        partNo:
          type: string
        lineItem:
          type: string
          description: Spaces are stripped automatically by the server
        description:
          type: string
          description: Item description / توصيف البند
        uom:
          type: string
        qty:
          type: number
        unitPrice:
          type: number
          description: Unit price entered after the RFQ is saved (required to finalize)

    CustomerRfqLineItem:
      type: object
      required: [id, customerRfqId, createdAt]
      properties:
        id:
          type: integer
        customerRfqId:
          type: integer
        partNo:
          type: ["string", "null"]
        lineItem:
          type: ["string", "null"]
        description:
          type: ["string", "null"]
        uom:
          type: ["string", "null"]
        qty:
          type: ["number", "null"]
        unitPrice:
          type: ["number", "null"]
        total:
          type: ["number", "null"]
          description: qty * unitPrice, computed by the server
        hasPo:
          type: boolean
          description: True when this line item already appears on an issued customer PO (detail page highlights such rows green).
        createdAt:
          type: string

    CustomerRfqInput:
      type: object
      required: [customerName]
      properties:
        customerId:
          type: ["integer", "null"]
        customerName:
          type: string
        customerRfqNo:
          type: string
          description: Optional — auto-generated when blank (numberAutoGenerated becomes true)
        entryDate:
          type: string
        expiryDate:
          type: string
        buyerName:
          type: string
        notes:
          type: string
        items:
          type: array
          items:
            $ref: "#/components/schemas/CustomerRfqLineItemInput"

    CustomerRfqUpdate:
      type: object
      properties:
        customerName:
          type: string
        customerRfqNo:
          type: string
        entryDate:
          type: string
        expiryDate:
          type: string
        buyerName:
          type: string
        notes:
          type: string
        status:
          type: string
        items:
          type: array
          items:
            $ref: "#/components/schemas/CustomerRfqLineItemInput"
        overrideMarginCheck:
          type: boolean
          description: Admin-only — bypass the 1.06× margin check on finalize (audited).

    CustomerRfqDetail:
      allOf:
        - $ref: "#/components/schemas/CustomerRfq"
        - type: object
          properties:
            items:
              type: array
              items:
                $ref: "#/components/schemas/CustomerRfqLineItem"

    # ── Customer PO ──────────────────────────────────────────────────────────
    CustomerPo:
      type: object
      required: [id, internalPoNo, customerPoNo, status, createdAt, updatedAt]
      properties:
        id:
          type: integer
        internalPoNo:
          type: string
          description: Auto-generated internal number (CPO-YYYY-NNNNNN)
        customerPoNo:
          type: string
          description: PO number received from the customer
        customerId:
          type: ["integer", "null"]
          description: The owning customer (explicitly selected on entry)
        customerName:
          type: ["string", "null"]
          description: The owning customer's name (stored, not derived)
        poDate:
          type: ["string", "null"]
        buyerName:
          type: ["string", "null"]
          description: The customer's buyer/contact reference (separate from the entering employee)
        employeeId:
          type: ["integer", "null"]
          description: Employee who entered the PO (auto from session)
        employeeName:
          type: ["string", "null"]
        status:
          type: string
          description: Stored lifecycle status (draft | sent). The customer-PO finalization flag, NOT the fulfillment progress.
        fulfillmentStatus:
          $ref: "#/components/schemas/CustomerPoFulfillmentStatus"
        notes:
          type: ["string", "null"]
        itemCount:
          type: integer
        createdAt:
          type: string
        updatedAt:
          type: string

    CustomerPoFulfillmentStatus:
      type: object
      description: >
        Derived fulfillment progress for a customer PO, computed by the server
        from the linked supplier purchase orders, the receipts recorded from the
        supplier, and the deliveries made to the customer.
      required: [stage, label]
      properties:
        stage:
          type: string
          enum: [draft, sent, po_issued, ready_to_deliver, delivered, fulfilled]
          description: >
            draft — customer PO not yet finalized.
            sent — finalized but no supplier PO dispatched yet.
            po_issued — at least one linked supplier PO has been dispatched (sent to the supplier).
            ready_to_deliver — some line items received from the supplier but none delivered yet.
            delivered — deliveries recorded; partial when deliveredPct < 100.
            fulfilled — all line items resolved (delivered OR rejected by customer).
        label:
          type: string
          description: Arabic label ready to display, e.g. "مسودة", "تم الإرسال", "تم إصدار أمر شراء للمورد", "جاهز للتسليم 50%", "تم تنفيذه 60%", "اكتمل".
        poIssued:
          type: boolean
          description: True when at least one dispatched (status=sent) supplier PO is linked to this customer PO.
        totalItems:
          type: integer
          description: Total number of line items on this customer PO.
        receivedItems:
          type: integer
          description: Number of line items received from the supplier (linked supplier PO item accepted).
        receivedPct:
          type: ["integer", "null"]
          description: Share (0–100) of line items received from the supplier. Null when the customer PO has no items.
        deliveredItems:
          type: integer
          description: Number of line items fully delivered to the customer (deliveryStatus = delivered).
        deliveredPct:
          type: ["integer", "null"]
          description: Share (0–100) of line items resolved (delivered OR rejected by the customer). Null when the customer PO has no items.

    CustomerPoCustomerRfqOption:
      type: object
      required: [id, customerRfqNo, internalNo, customerName, status]
      properties:
        id:
          type: integer
        customerRfqNo:
          type: string
        internalNo:
          type: string
        customerName:
          type: string
        status:
          type: string

    CustomerPoLineItemInput:
      type: object
      properties:
        customerRfqId:
          type: ["integer", "null"]
          description: The customer RFQ this line was sourced from (null for free/manual lines)
        customerRfqItemId:
          type: ["integer", "null"]
          description: The specific customer RFQ line item. Not unique — the same item may be ordered again on a later PO.
        partNo:
          type: string
        lineItem:
          type: string
          description: Spaces are stripped automatically by the server
        description:
          type: string
        uom:
          type: string
        qty:
          type: number
        unitPrice:
          type: number
        deliveryDate:
          type: string
          description: Required delivery date for this line

    CustomerPoLineItem:
      type: object
      required: [id, customerPoId, createdAt]
      properties:
        id:
          type: integer
        customerPoId:
          type: integer
        customerRfqId:
          type: ["integer", "null"]
        customerRfqItemId:
          type: ["integer", "null"]
        customerRfqNo:
          type: ["string", "null"]
          description: The customer RFQ number linked to this item (resolved by the server)
        partNo:
          type: ["string", "null"]
        lineItem:
          type: ["string", "null"]
        description:
          type: ["string", "null"]
        uom:
          type: ["string", "null"]
        qty:
          type: ["number", "null"]
        unitPrice:
          type: ["number", "null"]
        total:
          type: ["number", "null"]
          description: qty * unitPrice, computed by the server
        deliveryDate:
          type: ["string", "null"]
        highlightColor:
          type: ["string", "null"]
          enum: [yellow, green, blue, red, orange, purple, null]
          description: Manual row tint on /items (set via PATCH /customer-po/items/:id/highlight)
        highlightNote:
          type: ["string", "null"]
          description: Manual note shown in the /items «السبب» column
        createdAt:
          type: string

    CustomerPoInput:
      type: object
      required: [customerPoNo, customerName, items]
      properties:
        customerPoNo:
          type: string
        customerId:
          type: ["integer", "null"]
        customerName:
          type: string
          description: The owning customer's name (explicitly selected/typed)
        poDate:
          type: string
        buyerName:
          type: string
          description: Optional customer-side buyer/contact reference
        notes:
          type: string
        items:
          type: array
          items:
            $ref: "#/components/schemas/CustomerPoLineItemInput"

    CustomerPoUpdate:
      type: object
      properties:
        customerPoNo:
          type: string
        customerId:
          type: ["integer", "null"]
        customerName:
          type: string
        poDate:
          type: string
        buyerName:
          type: string
        notes:
          type: string
        status:
          type: string
        items:
          type: array
          items:
            $ref: "#/components/schemas/CustomerPoLineItemInput"

    CustomerPoDetail:
      allOf:
        - $ref: "#/components/schemas/CustomerPo"
        - type: object
          properties:
            items:
              type: array
              items:
                $ref: "#/components/schemas/CustomerPoLineItem"

    BulkImportSuppliersInput:
      type: object
      required: [suppliers]
      properties:
        suppliers:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/SupplierInput"

    BulkImportSuppliersResult:
      type: object
      required: [imported, skipped, errors]
      properties:
        imported:
          type: integer
          description: "عدد الموردين الذين تمت إضافتهم بنجاح"
        skipped:
          type: integer
          description: "عدد الموردين الذين تخطيناهم (مكررين)"
        errors:
          type: integer
          description: "عدد الموردين الذين فشل استيرادهم"
        details:
          type: array
          items:
            type: object
            properties:
              row:
                type: integer
              name:
                type: string
              status:
                type: string
                enum: [imported, skipped, error]
              reason:
                type: ["string", "null"]
              supplier:
                $ref: "#/components/schemas/Supplier"

    SupplierScore:
      type: object
      required: [supplierId]
      properties:
        supplierId:
          type: integer
        supplierName:
          type: string
        totalScore:
          type: ["number", "null"]
          description: "الدرجة الإجمالية المرجّحة فوق المكونات المتاحة (0-100)، null إن لم توجد بيانات"
        rating:
          type: ["number", "null"]
          description: "التقييم بالنجوم (0-5)"
        # مكونات التقييم — كل منها قابل أن يكون null عندما لا تتوفر بياناته
        commitmentScore:
          type: ["number", "null"]
          description: "نسبة الاستجابة (0-100)"
        responseSpeedScore:
          type: ["number", "null"]
          description: "سرعة الرد (0-100)"
        priceScore:
          type: ["number", "null"]
          description: "السعر — مقارنةً بمتوسط السوق (0-100)"
        winScore:
          type: ["number", "null"]
          description: "نسبة الفوز — من العروض إلى أوامر الشراء (0-100)"
        receiptQualityScore:
          type: ["number", "null"]
          description: "جودة الاستلام — مقبل/مرفوض (0-100)"
        deliveryScore:
          type: ["number", "null"]
          description: "مدة التوريد (0-100)"
        # إحصاءات
        totalRfqsReceived:
          type: integer
          description: "إجمالي طلبات عروض الأسعار المستلمة"
        totalOffersSubmitted:
          type: integer
          description: "إجمالي العروض المقدمة"
        responseRate:
          type: ["number", "null"]
          description: "نسبة الاستجابة (%)"
        avgResponseHours:
          type: ["number", "null"]
          description: "متوسط ساعات الرد على الطلب"
        totalItemsOffered:
          type: integer
          description: "إجمالي بنود العروض المقدمة"
        wins:
          type: integer
          description: "عدد بنود الفوز (تحوّلت إلى أمر شراء)"
        acceptedQty:
          type: number
          description: "إجمالي الكمية المقبولة من المورد"
        rejectedQty:
          type: number
          description: "إجمالي الكمية المرفوضة من المورد"
        avgDeliveryDays:
          type: ["integer", "null"]
          description: "متوسط مدة التوريد بالأيام"
        avgPriceDelta:
          type: ["number", "null"]
          description: "متوسط انحراف السعر عن متوسط السوق (%)"

    # RFQ
    Rfq:
      type: object
      required: [id, internalRfqNo, customerRfqNo, status, createdAt]
      properties:
        id:
          type: integer
        internalRfqNo:
          type: string
        customerRfqNo:
          type: string
        customerRfqDate:
          type: ["string", "null"]
        requiredResponseDate:
          type: ["string", "null"]
        status:
          type: string
          enum: [DRAFT, SENT, QUOTED, FAILED, SUCCESS]
        employeeId:
          type: integer
        employeeName:
          type: ["string", "null"]
        notes:
          type: ["string", "null"]
        expiresAt:
          type: ["string", "null"]
        itemCount:
          type: integer
        supplierCount:
          type: integer
        offerCount:
          type: integer
        createdAt:
          type: string
        updatedAt:
          type: string

    RfqInput:
      type: object
      required: [customerRfqNo]
      properties:
        customerRfqNo:
          type: string
        notes:
          type: string
        expiresAt:
          type: ["string", "null"]
          description: ISO datetime after which supplier pricing is blocked

    RfqUpdate:
      type: object
      properties:
        status:
          type: string
          enum: [DRAFT, SENT, QUOTED, FAILED, SUCCESS]
        notes:
          type: string

    RfqItem:
      type: object
      required: [id, rfqId, description]
      properties:
        id:
          type: integer
        rfqId:
          type: integer
        itemId:
          type: ["string", "null"]
        lineItem:
          type: ["string", "null"]
        partNo:
          type: ["string", "null"]
        description:
          type: string
        uom:
          type: ["string", "null"]
        qty:
          type: ["number", "null"]
        referencePrice:
          type: ["number", "null"]

    CustomerRfqItem:
      type: object
      required: [description]
      properties:
        itemId:
          type: string
        customerRfqItemId:
          type: integer
          description: FK to customer_rfq_items.id (present when sourced from a DB customer RFQ).
        lineItem:
          type: string
        partNo:
          type: string
        description:
          type: string
        uom:
          type: string
        qty:
          type: number
        referencePrice:
          type: ["number", "null"]
        rfqNo:
          type: string
        rfqDate:
          type: string
        requiredResponseDate:
          type: string

    SendRfqInput:
      type: object
      required: [supplierIds]
      properties:
        supplierIds:
          type: array
          items:
            type: integer
        closeDate:
          type: string
        notes:
          type: string

    SendRfqResult:
      type: object
      required: [sent, skipped]
      properties:
        sent:
          type: integer
        skipped:
          type: integer
        details:
          type: array
          items:
            type: object
            properties:
              supplierId:
                type: integer
              supplierName:
                type: string
              status:
                type: string
              reason:
                type: ["string", "null"]

    SentLogEntry:
      type: object
      required: [id, rfqId, supplierId, token, createdAt]
      properties:
        id:
          type: integer
        rfqId:
          type: integer
        internalRfqNo:
          type: string
        supplierId:
          type: integer
        supplierName:
          type: string
        contactPerson:
          type: ["string", "null"]
        email:
          type: ["string", "null"]
        phone:
          type: ["string", "null"]
        employeeId:
          type: integer
        employeeName:
          type: ["string", "null"]
        token:
          type: string
        closeDate:
          type: ["string", "null"]
        linkOpened:
          type: boolean
        openCount:
          type: integer
        firstOpenedAt:
          type: ["string", "null"]
        lastOpenedAt:
          type: ["string", "null"]
        offerSubmitted:
          type: boolean
        createdAt:
          type: string

    # PURCHASE ORDERS
    PurchaseOrder:
      type: object
      required: [id, internalPoNo, sheetPoNo, status, createdAt]
      properties:
        id:
          type: integer
        internalPoNo:
          type: string
        sheetPoNo:
          type: string
        receiverName:
          type: ["string", "null"]
        receiverPhone:
          type: ["string", "null"]
        status:
          type: string
        employeeId:
          type: integer
        employeeName:
          type: ["string", "null"]
        notes:
          type: ["string", "null"]
        itemCount:
          type: integer
        createdAt:
          type: string
        updatedAt:
          type: string

    PurchaseOrderInput:
      type: object
      required: [sheetPoNo, items]
      properties:
        sheetPoNo:
          type: string
        receiverName:
          type: string
        receiverPhone:
          type: string
        notes:
          type: string
        items:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/PurchaseOrderItemInput"

    PurchaseOrderItemInput:
      type: object
      required: [description]
      properties:
        itemId:
          type: string
        lineItem:
          type: string
        partNo:
          type: string
        description:
          type: string
        uom:
          type: string
        qty:
          type: ["number", "null"]
        referencePrice:
          type: ["number", "null"]
        supplierId:
          type: ["integer", "null"]
        taxIncluded:
          type: boolean
          default: false

    PurchaseOrderUpdateInput:
      type: object
      required: [sheetPoNo, items]
      properties:
        sheetPoNo:
          type: string
        receiverName:
          type: ["string", "null"]
        receiverPhone:
          type: ["string", "null"]
        notes:
          type: ["string", "null"]
        employeeId:
          type: ["integer", "null"]
          description: Responsible employee (المندوب / المسؤول)
        items:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/PurchaseOrderItemInput"

    PurchaseOrderItem:
      type: object
      required: [id, poId, description]
      properties:
        id:
          type: integer
        poId:
          type: integer
        supplierId:
          type: ["integer", "null"]
        supplierName:
          type: ["string", "null"]
        itemId:
          type: ["string", "null"]
        lineItem:
          type: ["string", "null"]
        partNo:
          type: ["string", "null"]
        description:
          type: string
        uom:
          type: ["string", "null"]
        qty:
          type: ["number", "null"]
        referencePrice:
          type: ["number", "null"]
        taxIncluded:
          type: boolean

    SheetPoItem:
      type: object
      required: [description]
      properties:
        itemId:
          type: string
        lineItem:
          type: string
        partNo:
          type: string
        description:
          type: string
        uom:
          type: string
        qty:
          type: ["number", "null"]
        referencePrice:
          type: ["number", "null"]
        poNo:
          type: string

    # OFFERS / PRICING
    PricingPageData:
      type: object
      required: [rfqNo, supplierName, items, closeDate, isExpired]
      properties:
        rfqNo:
          type: string
        supplierName:
          type: string
        contactPerson:
          type: ["string", "null"]
        items:
          type: array
          items:
            $ref: "#/components/schemas/RfqItem"
        closeDate:
          type: string
        isExpired:
          type: boolean
        alreadySubmitted:
          type: boolean
        existingOffer:
          $ref: "#/components/schemas/Offer"

    OfferInput:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            type: object
            required: [rfqItemId, price]
            properties:
              rfqItemId:
                type: integer
              price:
                type: number
              taxIncluded:
                type: boolean
              deliveryDays:
                type: integer
              notes:
                type: string
        generalNotes:
          type: string

    Offer:
      type: object
      required: [id, rfqId, supplierId, createdAt]
      properties:
        id:
          type: integer
        rfqId:
          type: integer
        supplierId:
          type: integer
        supplierName:
          type: ["string", "null"]
        sentLogId:
          type: integer
        employeeId:
          type: ["integer", "null"]
        totalPrice:
          type: ["number", "null"]
        generalNotes:
          type: ["string", "null"]
        createdAt:
          type: string
        items:
          type: array
          items:
            $ref: "#/components/schemas/OfferItem"

    OfferItem:
      type: object
      required: [id, offerId, rfqItemId, price]
      properties:
        id:
          type: integer
        offerId:
          type: integer
        rfqItemId:
          type: integer
        partNo:
          type: ["string", "null"]
        description:
          type: ["string", "null"]
        qty:
          type: ["number", "null"]
        uom:
          type: ["string", "null"]
        price:
          type: number
        taxIncluded:
          type: boolean
        isApproved:
          type: boolean
        deliveryDays:
          type: ["integer", "null"]
        notes:
          type: ["string", "null"]

    RfqOffersResponse:
      type: object
      required: [rfq, offers, analysis]
      properties:
        rfq:
          $ref: "#/components/schemas/Rfq"
        offers:
          type: array
          items:
            $ref: "#/components/schemas/Offer"
        analysis:
          $ref: "#/components/schemas/PriceAnalysis"

    # ANALYTICS
    DashboardStats:
      type: object
      required: [totalRfqs, openRfqs, totalSuppliers, totalOffers]
      properties:
        totalRfqs:
          type: integer
        openRfqs:
          type: integer
        totalSuppliers:
          type: integer
        totalOffers:
          type: integer
        rfqsByStatus:
          type: array
          items:
            type: object
            properties:
              status:
                type: string
              count:
                type: integer
        recentRfqs:
          type: array
          items:
            $ref: "#/components/schemas/Rfq"
        topSuppliers:
          type: array
          items:
            $ref: "#/components/schemas/SupplierScore"
        responseRateThisMonth:
          type: number
        avgResponseTimeHours:
          type: number
        # Pricing & PO analytics
        totalItems:
          type: integer
          description: Total RFQ line items across all RFQs
        pricedItems:
          type: integer
          description: Items that received at least one offer price
        unpricedItems:
          type: integer
          description: Items with no offers received
        itemsWithPo:
          type: integer
          description: Items that appear in a purchase order
        pricingRate:
          type: number
          description: Ratio of priced items to total items (0-100)
        poRate:
          type: number
          description: Ratio of items with PO to total items (0-100)
        totalPos:
          type: integer
          description: Total purchase orders created
        rfqsWithPo:
          type: integer
          description: Number of RFQs that have a linked PO
        rfqToPoRate:
          type: number
          description: Ratio of RFQs that converted to a PO (0-100)
        supplierDeepStats:
          type: array
          description: Enhanced supplier analytics
          items:
            type: object
            properties:
              supplierId:
                type: integer
              supplierName:
                type: string
              category:
                type: string
              totalRfqsReceived:
                type: integer
              totalOffersSubmitted:
                type: integer
              responseRate:
                type: number
              totalItemsOffered:
                type: integer
              totalPoItems:
                type: integer
              poWinRate:
                type: number
                description: Ratio of items awarded PO out of offers submitted
              avgPrice:
                type: ["number", "null"]
              avgDeliveryDays:
                type: ["number", "null"]

    EmployeePerformance:
      type: object
      required: [employee]
      properties:
        employee:
          $ref: "#/components/schemas/Employee"
        totalRfqsSent:
          type: integer
        totalOffersReceived:
          type: integer
        responseRate:
          type: number
        avgSendTimeHours:
          type: number
        totalPurchaseValue:
          type: number
        awardRate:
          type: number
        rfqsByMonth:
          type: array
          items:
            type: object
            properties:
              month:
                type: string
              count:
                type: integer

    PriceAnalysis:
      type: object
      required: [rfqId]
      properties:
        rfqId:
          type: integer
        itemAnalysis:
          type: array
          items:
            type: object
            properties:
              rfqItemId:
                type: integer
              description:
                type: string
              partNo:
                type: ["string", "null"]
              qty:
                type: ["number", "null"]
              uom:
                type: ["string", "null"]
              referencePrice:
                type: ["number", "null"]
              minPrice:
                type: ["number", "null"]
              maxPrice:
                type: ["number", "null"]
              avgPrice:
                type: ["number", "null"]
              fairPrice:
                type: ["number", "null"]
              offers:
                type: array
                items:
                  type: object
                  properties:
                    offerItemId:
                      type: integer
                    supplierId:
                      type: integer
                    supplierName:
                      type: string
                    price:
                      type: number
                    taxIncluded:
                      type: boolean
                    isApproved:
                      type: boolean
                    deliveryDays:
                      type: ["integer", "null"]
                    deviation:
                      type: number
                    isLowest:
                      type: boolean
                    isAnomaly:
                      type: boolean
                    notes:
                      type: ["string", "null"]
                    priceWithVat:
                      type: number
                    notPriced:
                      type: boolean

    # ITEM HISTORY (search result)
    ItemSupplierResponse:
      type: object
      required: [supplierId, supplierName, responded, offerSubmitted, linkOpened]
      properties:
        supplierId:
          type: integer
        supplierName:
          type: string
        responded:
          type: boolean
        offerSubmitted:
          type: boolean
        linkOpened:
          type: boolean
        price:
          type: ["number", "null"]
        taxIncluded:
          type: boolean
        deliveryDays:
          type: ["integer", "null"]
        notes:
          type: ["string", "null"]
        offerDate:
          type: ["string", "null"]

    ItemHistory:
      type: object
      required:
        [
          rfqItemId,
          description,
          rfqId,
          internalRfqNo,
          customerRfqNo,
          rfqStatus,
          rfqDate,
          sentCount,
          respondedCount,
          notRespondedCount,
          suppliers,
        ]
      properties:
        rfqItemId:
          type: integer
        lineItem:
          type: ["string", "null"]
        partNo:
          type: ["string", "null"]
        description:
          type: string
        uom:
          type: ["string", "null"]
        qty:
          type: ["number", "null"]
        referencePrice:
          type: ["number", "null"]
        rfqId:
          type: integer
        internalRfqNo:
          type: string
        customerRfqNo:
          type: string
        rfqStatus:
          type: string
        rfqDate:
          type: string
        rfqResponseDate:
          type: ["string", "null"]
        employeeName:
          type: ["string", "null"]
        sentCount:
          type: integer
        respondedCount:
          type: integer
        notRespondedCount:
          type: integer
        minPrice:
          type: ["number", "null"]
        maxPrice:
          type: ["number", "null"]
        avgPrice:
          type: ["number", "null"]
        suppliers:
          type: array
          items:
            $ref: "#/components/schemas/ItemSupplierResponse"

    # ERP INTEGRATIONS
    ErpIntegration:
      type: object
      required: [id, name, type, isActive, createdAt, updatedAt]
      properties:
        id:
          type: integer
        name:
          type: string
          description: "اسم التكامل (مثال: Odoo الإنتاج)"
        type:
          type: string
          enum: [odoo, sap-b1, sap-s4hana, oracle, google-sheets]
          description: "نوع نظام الـ ERP"
        config:
          type: object
          description: "إعدادات الاتصال (كلمات المرور مخفاة)"
          additionalProperties: true
        isActive:
          type: boolean
        lastSyncAt:
          type: ["string", "null"]
        lastSyncStatus:
          type: ["string", "null"]
          enum: [success, error, partial, null]
        lastSyncError:
          type: ["string", "null"]
        lastSyncStats:
          type: ["object", "null"]
          additionalProperties: true
        createdAt:
          type: string
        updatedAt:
          type: string

    ErpIntegrationInput:
      type: object
      required: [name, type, config]
      properties:
        name:
          type: string
        type:
          type: string
          enum: [odoo, sap-b1, sap-s4hana, oracle, google-sheets]
        config:
          type: object
          additionalProperties: true
          description: |
            إعدادات الاتصال حسب النوع:
            - odoo: { url, db, username, apiKey }
            - sap-b1: { url, username, password, companyDB }
            - sap-s4hana: { url, username, password }
            - oracle: { url, username, password, businessUnit? }
            - google-sheets: { serviceAccountBase64?, spreadsheetId, dataSheetName? }

    ErpConnectionTestResult:
      type: object
      required: [ok]
      properties:
        ok:
          type: boolean
        version:
          type: string
        error:
          type: ["string", "null"]

    ErpSyncResult:
      type: object
      properties:
        success:
          type: boolean
        message:
          type: string
        suppliersImported:
          type: integer
        suppliersUpdated:
          type: integer
        suppliersSkipped:
          type: integer
        rfqsExported:
          type: integer
        posExported:
          type: integer
        error:
          type: ["string", "null"]

    # AUDIT
    AuditLog:
      type: object
      required: [id, action, createdAt]
      properties:
        id:
          type: integer
        action:
          type: string
        entityType:
          type: ["string", "null"]
        entityId:
          type: ["integer", "null"]
        employeeId:
          type: ["integer", "null"]
        employeeName:
          type: ["string", "null"]
        description:
          type: string
        ipAddress:
          type: ["string", "null"]
        userAgent:
          type: ["string", "null"]
        createdAt:
          type: string

    # REPORTS
    ReportsData:
      type: object
      properties:
        period:
          type: object
          properties:
            from:
              type: ["string", "null"]
            to:
              type: ["string", "null"]
        summary:
          type: object
          properties:
            totalRfqs:
              type: integer
            totalPos:
              type: integer
            totalItems:
              type: integer
            conversionRate:
              type: number
        employeeStats:
          type: array
          items:
            type: object
            properties:
              employeeId:
                type: integer
              employeeName:
                type: string
              role:
                type: string
              totalRfqs:
                type: integer
              totalOffers:
                type: integer
              totalPos:
                type: integer
              successRfqs:
                type: integer
              conversionRate:
                type: number
        topItems:
          type: array
          items:
            type: object
            properties:
              description:
                type: string
              partNo:
                type: ["string", "null"]
              lineItem:
                type: ["string", "null"]
              count:
                type: integer
              totalQty:
                type: number
        lineItemStats:
          type: array
          items:
            type: object
            properties:
              lineItem:
                type: string
              count:
                type: integer
              distinctRfqs:
                type: integer
        monthlyTrend:
          type: array
          items:
            type: object
            properties:
              month:
                type: string
              rfqs:
                type: integer
              pos:
                type: integer
        statusFunnel:
          type: array
          items:
            type: object
            properties:
              status:
                type: string
              count:
                type: integer
